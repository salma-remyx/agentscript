{
  "name": "@agentscript/agentforce-dialect",
  "version": "2.13.5",
  "description": "Agentforce dialect — extends AgentScript with Salesforce-specific blocks, fields, and lint rules",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "sync-meta": "node ../../scripts/sync-pkg-meta.mjs",
    "build": "yarn sync-meta && tsc",
    "dev": "tsc --watch",
    "test": "yarn sync-meta && vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=lcov --coverage.reporter=json-summary --coverage.reportsDirectory=coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentscript/agentscript-dialect": "workspace:*",
    "@agentscript/language": "workspace:*",
    "@agentscript/types": "workspace:*"
  },
  "devDependencies": {
    "@agentscript/parser": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^3.2.6"
  },
  "keywords": [
    "agentscript",
    "agentforce",
    "dialect",
    "salesforce"
  ],
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  }
}

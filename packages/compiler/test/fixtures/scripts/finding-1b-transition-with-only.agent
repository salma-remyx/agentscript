config:
    agent_name: "Finding1b"

start_agent main:
    description: "transition has `with` clauses but no `to` target — should still be rejected"
    before_reasoning:
        transition with foo="bar"
    reasoning:
        instructions: ->
            | hello

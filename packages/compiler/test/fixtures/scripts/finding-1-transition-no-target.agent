config:
    agent_name: "Finding1"

start_agent main:
    description: "Bare `transition` (no `to <target>`) — should be rejected per SPEC §5.10"
    before_reasoning:
        transition
    reasoning:
        instructions: ->
            | hello
